# PHPUnit Configuration for Discogs API Bundle

parameters:
    env(DISCOGS_USER_TOKEN): ''
    env(DISCOGS_CONSUMER_KEY): ''
    env(DISCOGS_CONSUMER_SECRET): ''
    env(DISCOGS_TOKEN): ''
    env(DISCOGS_TOKEN_SECRET): ''

coverage:
    include:
        - '../src/*'
